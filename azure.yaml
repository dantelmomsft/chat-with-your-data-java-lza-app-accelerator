# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: chat-with-your-data-java-lza-app-accelerator
metadata:
  template: chat-with-your-data-java-lza-app-accelerator@0.0.1-snapshot

hooks:
    prerestore:
        posix:
        shell: sh
        run: echo 'I should download the app code here'
        interactive: true
        continueOnError: false
    prepackage:
        posix:
        shell: sh
        run: echo 'running before package'
        interactive: true
        continueOnError: false
    preprovision:
        posix:
        shell: sh
        run: ./scripts/download-asa-multitenant.sh --branch main
        interactive: true
        continueOnError: false
